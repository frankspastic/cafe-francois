{
  "name": "server",
  "version": "1.0.0",
  "description": "Express backend for Cafe Francois",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon --exec \"node --env-file=../.env\" server.js",
    "start": "node --env-file-if-exists=../.env server.js",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.6.2",
    "sql.js": "^1.8.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
